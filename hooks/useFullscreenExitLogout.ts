/**
 * Fullscreen exit logout has been disabled.
 * Teams are no longer logged out when they exit fullscreen.
 * Only the explicit logout button triggers logout.
 */
export function useFullscreenExitLogout() {
    // no-op
}
